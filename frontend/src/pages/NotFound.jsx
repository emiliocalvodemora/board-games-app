import React from 'react'

// Página a la que se redirige cuando el usuario intenta acceder a una ruta que no existe
const NotFound = () => {
  return (
    <div className="auth-error-404">404 not found</div>
  )
}

export default NotFound